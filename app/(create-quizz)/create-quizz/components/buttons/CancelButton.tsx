import { Button } from "@nextui-org/react";
import React from "react";

function CancelButton() {
  return (
    <Button variant="flat" color="primary" size="lg" radius="sm">
      Cancel
    </Button>
  );
}

export default CancelButton;
